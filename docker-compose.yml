services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: landscape_design
      POSTGRES_USER: landscape
      POSTGRES_PASSWORD: landscape
    ports:
      - "5432:5432"
    volumes:
      - landscape_pg_data:/var/lib/postgresql/data

volumes:
  landscape_pg_data:
